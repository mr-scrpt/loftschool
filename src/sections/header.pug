header.header.page__header
  +image('parallax/bg__back-plan.png', 'header__bg img')
  .header__arrow.arrow
    .arrow__scroll
      span.arrow__text Скролл
      +icon(`icon__arrow-down`, 'icon arrow__icon')
  .header__inner
    .header__topbar
      .header__logo ДЛ
      nav.header__menu.menu-top
        -let menu = ['Главная', 'Обо мне','Навыки','Работы','Отзывы','Связаться']
        each item in menu
          a(href=`#${item}`, class="menu-top__link")=item
      .header__soc.soc-block.soc-block_header
        .soc-block__inner
          -let soc = {insta: "instagram.com", vk: "vk.com" ,gh: "github.com"}
          each val, index in soc
            a(href=`${val}`, class="soc-block__link")
              +icon(`icon__${index}`, `icon icon_${index} soc-block__icon`)
    .header__me.me
      .me__inner
        .me__info
          .me__line Личный сайт
          .me__line веб разработчика
        .me__name Дмитрий Лукьянов